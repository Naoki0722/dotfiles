# dotfiles

Simple dotfiles includes .zsh with Prezto, .vimrc with NeoBundle, etc.

## Brew関係の導入
```
# brew でのインストール結果を Brewfile に書き出す
$ brew bundle dump

# Brewfile を元にインストール
$ brew bundle install
```
